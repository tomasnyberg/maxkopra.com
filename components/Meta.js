import Head from 'next/head'

const Meta = ({ title, keywords, description }) => {
  return (
    <Head>
      <meta name='viewport' content='width=device-width, initial-scale=1' />
      <meta name='keywords' content={keywords} />
      <meta name='description' content={description} />
      <meta charSet='utf-8' />
      <link rel='icon' href='/favicon.ico' />
      <title>{title}</title>
    </Head>
  )
}

Meta.defaultProps = {
  title: 'Max Kopra',
  keywords: 'philosophy, writing, philosophy writing, philosophy blog, philosophy thoughts, litterature, blog, thoughts',
  description: 'Personal blog of (to be) author Max Kopra',
}

export default Meta